+++
title = "{{ replace .File.ContentBaseName "-" " " | title }}"
description = ""
# Sidebar and prev/next order within the section. Leave gaps of 10.
weight = 10
# Icon for the parent section's overview card — any name in assets/icons/.
icon = "file"
tags = []
draft = true
+++

Open with what the reader can do after this page, in one or two sentences.

## First heading

Write prose, lists, code fences and links as ordinary Markdown. Reach for a
shortcode only where a component needs structured children — callouts, cards,
tabs, steps, file trees.
