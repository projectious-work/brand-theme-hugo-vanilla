# aibox tmux configuration
set -g default-shell /bin/bash
set -g default-command /bin/bash
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g mouse on
set -g history-limit 50000
set -g escape-time 10
set -g focus-events on
set -g xterm-keys on
set -s extended-keys on
set -g allow-passthrough on
set -g set-clipboard external
set -g mode-keys vi
set -g default-terminal "tmux-256color"
set -ga terminal-features ",*:extkeys,wezterm:RGB,clipboard,xterm-256color:RGB,clipboard,tmux-256color:RGB"
set -ga terminal-overrides ",xterm-256color:Tc,tmux-256color:Tc"
set -g status-interval 15
set -g prefix C-g
unbind C-b
bind C-g send-prefix

# Pane navigation mirrors the old aibox leader muscle memory.
bind-key -N "Show aibox/tmux key bindings" ? display-popup -w 110 -h 90% -E "bash \"$HOME/.local/bin/aibox-tmux-cheatsheet\""
bind-key -N "Select pane left" h select-pane -L
bind-key -N "Select pane down / next harness pane" j select-pane -D
bind-key -N "Select pane up / prev harness pane" k select-pane -U
bind-key -N "Select pane right" l select-pane -R
bind-key -N "Split pane right" r split-window -h -c "#{pane_current_path}"
bind-key -N "Split pane down" d split-window -v -c "#{pane_current_path}"
bind-key -N "Kill pane" x kill-pane
bind-key -N "Toggle pane zoom" f resize-pane -Z
bind-key -N "Toggle pane zoom (alias)" z resize-pane -Z
bind-key -N "Kill tmux session" q confirm-before -p "kill tmux session brand-theme-hugo-vanilla? (y/n)" kill-session
bind-key -N "Reload tmux config" R source-file ~/.config/tmux/tmux.conf \; display-message "aibox tmux config reloaded"
bind-key -N "Open log pane (lnav)" o display-popup -E -w 90% -h 80% "aibox-log-viewer"

# BR-TOOLS-AS-WINDOWS (BACK-20260510_0726-GrandDaisy, v0.25.7): one-letter
# prefix shortcuts to jump directly to named tool/harness windows.
# find-window -Z focuses the target window; silently no-ops when absent.
bind-key -N "Switch to lazygit window" g find-window -Z 'lazygit'
bind-key -N "Switch to shell window" s find-window -Z 'shell'

# Host paste sends line-feed bytes for newlines, and line-feed is C-j.
# Binding global C-j makes pasted multiline text corrupt in panes. Keep smart
# down navigation on C-Down and prefix j instead.
unbind-key -q -n C-j
unbind-key -q -T copy-mode-vi C-j
set -g @vim_navigator_mapping_down "C-Down"


bind-key -N "Choose layout" L display-menu -T "#[align=centre]Layout" -x C -y C \
  "Dev (work=files|harness)" d "run-shell 'aibox-tmux-confirm-and-switch dev'" \
  "AI (work=files|harness; ai windows)" a "run-shell 'aibox-tmux-confirm-and-switch ai'" \
  "Focus (one window per harness)" f "run-shell 'aibox-tmux-confirm-and-switch focus'" \
  "Cowork (files|shell + ai windows)" w "run-shell 'aibox-tmux-confirm-and-switch cowork'" \
  "" \
  "Cancel" q ""


bind-key -N "Choose theme" T display-menu -T "#[align=centre]Theme" -x C -y C \
  "gruvbox dark" g "run-shell 'aibox theme --theme gruvbox-dark && aibox-tmux-refresh-theme'" \
  "catppuccin mocha" c "run-shell 'aibox theme --theme catppuccin-mocha && aibox-tmux-refresh-theme'" \
  "tokyo night" t "run-shell 'aibox theme --theme tokyo-night && aibox-tmux-refresh-theme'" \
  "dracula" d "run-shell 'aibox theme --theme dracula && aibox-tmux-refresh-theme'" \
  "projectious" p "run-shell 'aibox theme --theme projectious && aibox-tmux-refresh-theme'" \
  "" \
  "Toggle light/dark" l "run-shell 'aibox theme --mode auto && aibox-tmux-refresh-theme'" \
  "" \
  "Heavy: restart TUIs (kill+respawn lazygit/lnav/AI)" R "run-shell 'AIBOX_THEME_CONFIRM_RESTART_TUIS=true aibox-tmux-refresh-theme --restart-tuis'" \
  "" \
  "Cancel" q ""


set -g status on
set -g status-style "bg=#1F1F28,fg=#DCD7BA"
set -g window-status-current-style "bg=#7E9CD8,fg=#1F1F28,bold"
set -g window-status-format " #I:#W "
set -g window-status-current-format " #I:#W "
# Inactive panes are dimmed a touch (bg+fg both biased ~12% toward
# muted) so the focused pane stands out without making non-focus
# content hard to read. Tweak via `themes::dim_inactive_pane_colors`.
set -g window-style "bg=#28282F,fg=#C1BDA5"
set -g window-active-style "bg=#1F1F28,fg=#DCD7BA"
set -g pane-border-style "fg=#727169,bg=#1F1F28"
set -g pane-active-border-style "fg=#7E9CD8,bg=#1F1F28"
set -g message-style "bg=#7E9CD8,fg=#1F1F28"
set -g message-command-style "bg=#7E9CD8,fg=#1F1F28"
set -g mode-style "bg=#7E9CD8,fg=#1F1F28"
set -g clock-mode-colour "#7E9CD8"
set -g popup-style "bg=#1F1F28,fg=#DCD7BA"
set -g popup-border-style "fg=#7E9CD8,bg=#1F1F28"
set -g status-left " #S #{W:#I:#W ,#[reverse]#I:#W#[noreverse] }"
set -g status-left-length 80
set -g status-right " #(aibox-status --once 2>/dev/null || true) %H:%M "

# Powerkit status. Theme: kanagawa-wave (custom palette)
set -g @powerkit_plugins "aibox_log,aibox_oom,aibox_proc,aibox_ai,aibox_mcp,aibox_mig,weather,uptime,datetime,forge,kubernetes,terraform,cloud,hostname,externalip,ssh,netspeed,ping,cpu,loadavg,memory,swap,disk,gpu"
set -g @powerkit_bar_layout "double"
set -g @powerkit_status_order "session,plugins"
set -g @powerkit_theme "custom"
set -g @powerkit_custom_theme_path "~/.config/tmux/aibox-powerkit-theme.sh"
set -g @powerkit_separator_style "rounded"
set -g @powerkit_edge_separator_style "rounded"
set -g @powerkit_elements_spacing "both"
set -g @powerkit_status_interval "15"
set -g @powerkit_transparent "false"
set -g @powerkit_pane_border_status "top"
set -g @powerkit_pane_border_unified "false"
set -g @powerkit_active_pane_border_color "#7E9CD8"
set -g @powerkit_inactive_pane_border_color "#727169"
set -g @powerkit_pane_border_status_bg "#1F1F28"
set -g @powerkit_pane_scrollbars_style_fg "#7E9CD8"
set -g @powerkit_pane_scrollbars_style_bg "#727169"
set -g @powerkit_pane_border_format "#{?client_prefix,PREFIX,NORMAL} #{pane_title} #{pane_current_command}"
set -g @powerkit_line1_right "aibox_log,aibox_oom,aibox_proc,aibox_ai,aibox_mcp,aibox_mig,weather,uptime,datetime"
set -g @powerkit_line2_left "forge,kubernetes,terraform,cloud"
set -g @powerkit_line2_right "hostname,externalip,ssh,netspeed,ping,cpu,loadavg,memory,swap,disk,gpu"
set -g @powerkit_plugin_netspeed_speed_width "7"
set -g @powerkit_plugin_aibox_log_metric "log"
set -g @powerkit_plugin_aibox_log_cache_ttl "30"
set -g @powerkit_plugin_aibox_oom_metric "oom"
set -g @powerkit_plugin_aibox_oom_cache_ttl "30"
set -g @powerkit_plugin_aibox_proc_metric "proc"
set -g @powerkit_plugin_aibox_proc_cache_ttl "30"
set -g @powerkit_plugin_aibox_ai_metric "ai"
set -g @powerkit_plugin_aibox_ai_cache_ttl "30"
set -g @powerkit_plugin_aibox_mcp_metric "mcp"
set -g @powerkit_plugin_aibox_mcp_cache_ttl "30"
set -g @powerkit_plugin_aibox_mig_metric "mig"
set -g @powerkit_plugin_aibox_mig_cache_ttl "30"
set -g @powerkit_plugin_netspeed_cache_ttl "10"
set -g @powerkit_plugin_kubernetes_cache_ttl "120"
set -g @powerkit_plugin_cloud_cache_ttl "120"
set -g @powerkit_plugin_forge_cache_ttl "120"
set -g @powerkit_plugin_forge_github_hosts "github.com"
set -g @powerkit_plugin_aibox_log_label "󱖫"
set -g @powerkit_plugin_aibox_oom_label "󰍛󰚌"
set -g @powerkit_plugin_aibox_proc_label "󰊚"
set -g @powerkit_plugin_aibox_ai_label "󱙺"
set -g @powerkit_plugin_aibox_mcp_label "󰌹"
set -g @powerkit_plugin_aibox_mig_label "󰚰"
set -g @powerkit_plugin_kubernetes_icon "󱃾"
set -g @powerkit_plugin_cloud_icon "󰅣"
set -g @powerkit_plugin_cloud_icon_aws "󰸏"
set -g @powerkit_plugin_cloud_icon_gcp "󰬠"
set -g @powerkit_plugin_cloud_icon_azure "󰠅"
set -g @powerkit_plugin_cloud_icon_multi "󰅤"
set -g @powerkit_plugin_uptime_icon ""
set -g @powerkit_plugin_netspeed_icon ""
set -g @powerkit_plugin_netspeed_icon_download "󰇚"
set -g @powerkit_plugin_netspeed_icon_upload "󰕒"

# aibox-managed plugins are installed and pinned by the runtime image. TPM is
# only a user convenience layer for additional personal plugins.
if-shell '[ -f /usr/local/share/aibox/tmux/plugins/tmux-sensible/sensible.tmux ]' 'run-shell /usr/local/share/aibox/tmux/plugins/tmux-sensible/sensible.tmux'
if-shell '[ -f /usr/local/share/aibox/tmux/plugins/tmux-powerkit/tmux-powerkit.tmux ]' 'run-shell "/usr/local/share/aibox/tmux/plugins/tmux-powerkit/tmux-powerkit.tmux; tmux source-file ~/.config/tmux/aibox-powerkit-overrides.tmux"'
if-shell '[ -f /usr/local/share/aibox/tmux/plugins/vim-tmux-navigator/vim-tmux-navigator.tmux ]' 'run-shell /usr/local/share/aibox/tmux/plugins/vim-tmux-navigator/vim-tmux-navigator.tmux'
if-shell '[ -f /usr/local/share/aibox/tmux/plugins/tmux-yank/yank.tmux ]' 'run-shell /usr/local/share/aibox/tmux/plugins/tmux-yank/yank.tmux'

# Powerkit renders these list-specific sections; tmux only supplies alignment.
set -g status 2
set -g 'status-format[0]' '#[align=left range=left #{E:status-left-style}]#[push-default]#(~/.local/bin/aibox-powerkit-render-session)#[pop-default]#[norange default]#[push-default]#{W:#[range=window|#{window_index} #{E:window-status-style}]#{T:window-status-format}#[norange default],#[range=window|#{window_index} #{E:window-status-current-style}]#{T:window-status-current-format}#[norange default]}#[pop-default]#{E:@_powerkit_left_edge_sep}#[nolist align=right range=right #{E:status-right-style}]#[push-default]#(~/.local/bin/aibox-powerkit-render-list right aibox_log,aibox_oom,aibox_proc,aibox_ai,aibox_mcp,aibox_mig,weather,uptime,datetime)#[pop-default]#[norange default]'
set -g 'status-format[1]' '#[align=left]#(~/.local/bin/aibox-powerkit-render-list left forge,kubernetes,terraform,cloud)#[align=right]#(~/.local/bin/aibox-powerkit-render-list right hostname,externalip,ssh,netspeed,ping,cpu,loadavg,memory,swap,disk,gpu)'

# aibox post-PowerKit overrides.
# PowerKit's renderer owns status-format and pane styles when it loads. Keep
# the aibox two-row status shape and pane surfaces authoritative after that
# render pass.
set -g pane-border-style "fg=#727169,bg=#1F1F28"
set -g pane-active-border-style "fg=#7E9CD8,bg=#1F1F28"
set -g pane-border-format "#[bg=#1F1F28]#{?pane_active,#[fg=#8CA4D3]#[bold],#[fg=#A7A491]} #{?client_prefix,PREFIX,NORMAL} #{pane_title} #{pane_current_command} #[bg=#1F1F28,fg=#1F1F28] "


# Persistence plugins are installed for later policy work but disabled by
# default in v0.25.0. Do not enable continuum/resurrect implicitly.
set -g @continuum-restore 'off'
set -g @continuum-save-interval '0'
set -g @resurrect-capture-pane-contents 'off'

if-shell '[ -x ~/.tmux/plugins/tpm/tpm ]' 'run-shell ~/.tmux/plugins/tpm/tpm'
