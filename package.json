{
  "name": "brand-theme-hugo-vanilla",
  "version": "0.3.0",
  "private": true,
  "description": "Build dependency for the Tailwind v4 CSS pipeline. Hugo invokes the CLI through css.TailwindCSS.",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0"
  }
}
